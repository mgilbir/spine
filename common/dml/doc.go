// Package dml provides DrawingML types shared across the Office Open XML
// formats: colors, fills, lines, effects, geometry, transforms, and text,
// plus the per-format drawing namespaces (picture, spreadsheet, word
// processing, and chart drawing) and the EMU measurement helpers.
package dml
