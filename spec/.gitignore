# ISO/IEC 29500 spec files (copyrighted, not redistributable)
part1/
part2/
part3/
part4/
msoi29500/

# NOTE: gen_spec/ is NOT ignored. It used to be, while its contents (Makefile,
# gen_spec.py, sql/, src_data/, enum/) were tracked — an ignore rule over
# tracked files only hides anything NEW added there from `git status`, which is
# how work gets silently lost. The directory is committed; keep it visible.
